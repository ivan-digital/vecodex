name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ macos-latest, ubuntu-latest ]

    steps:
    - uses: actions/checkout@v3

    - name: Install Dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew update
        brew install gcc libomp openblas faiss googletest cmake

    - name: Install Dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake libomp-dev libopenblas-dev libfaiss-dev libgtest-dev

    - name: Configure CMake
      run: |
        mkdir -p build
        cd build
        if [[ "$RUNNER_OS" == "macOS" ]]; then
          cmake -G "Xcode" -DCMAKE_BUILD_TYPE=Debug ..
        else
          cmake -DCMAKE_BUILD_TYPE=Debug ..
        fi

    - name: Build
      run: |
        cd build
        cmake --build . --config Debug

    - name: Run Tests
      run: |
        cd build
        ctest --output-on-failure
